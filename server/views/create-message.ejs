<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Create Message | myclubhouse</title>
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body class="bg-black text-white min-h-screen">
		<main class="max-w-2xl mx-auto px-4 py-8">
			<!-- Header -->
			<div class="mb-8 text-center">
				<h1 class="text-3xl font-bold text-white mb-2">
					Create New Message
				</h1>
				<% if (errors && errors.length > 0) { %>
  <% const generalError = errors.find(error => !error.param); %>
  <% if (generalError) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg relative mb-4" role="alert">
      <span class="block sm:inline"><%= generalError.msg %></span>
    </div>
  <% } %>
<% } %>
				<p class="text-gray-400">
					Share your thoughts with the community
				</p>
			</div>

			<!-- Message Creation Form -->
			<div class="bg-white rounded-lg shadow-xl p-8">
				<form
					action="/home/create-message"
					method="POST"
					class="space-y-6"
				>
					<!-- Username Field -->
					<div>
						<label
							for="username"
							class="block text-sm font-medium text-gray-700 mb-2"
						>
							Username
						</label>
						<input
							type="text"
							id="username"
							name="username"
							value = <%= user.email %>
							required
							placeholder="Enter your gmsil"
							class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900 placeholder-gray-500"
						/>
					</div>

					<!-- Message Title Field -->
					<div>
						<label
							for="title"
							class="block text-sm font-medium text-gray-700 mb-2"
						>
							Message Title
						</label>
						<input
							type="text"
							id="title"
							name="title"
							 
							placeholder="What's your message about?"
							class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors text-gray-900 placeholder-gray-500"
						/>
					</div>

					<!-- Message Content Field -->
					<div>
						<label
							for="message"
							class="block text-sm font-medium text-gray-700 mb-2"
						>
							Your Message
						</label>
						<textarea
							id="message"
							name="message"
							rows="6"
							required
							placeholder="Write your message here..."
							class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-colors resize-vertical text-gray-900 placeholder-gray-500"
						></textarea>
						<p class="text-sm text-gray-500 mt-2">
							Share your thoughts, ideas, or updates with the
							community.
						</p>
					</div>
					<!-- Form Actions -->
					<div class="flex flex-col sm:flex-row gap-4 pt-4">
						<a
							href="/home"
							type="button"
							class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium py-3 px-6 rounded-lg transition-colors text-center"
						>
							Cancel
						</a>
						<button
							type="submit"
							class="flex-1 bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-medium py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-[1.02] shadow-lg hover:shadow-xl"
						>
							Send Message
						</button>
					</div>
				</form>
			</div>

			<!-- Back to Home Link -->
			<div class="text-center mt-8">
				<a
					href="/home"
					class="text-blue-400 hover:text-blue-300 transition-colors"
				>
					← Back to Home
				</a>
			</div>
		</main>
	</body>
</html>
