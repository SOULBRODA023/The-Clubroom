<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>RIDDLE</title>
    <style>
      body {
        font-family: 'Inter', sans-serif;
      }
    </style>
  </head>
  <body
    class="min-h-screen bg-gray-900 flex items-center justify-center p-4 text-gray-100 antialiased"
  >
    <section
      class="max-w-md w-full bg-gray-800 p-8 rounded-xl shadow-lg border border-gray-700"
    >
      <header class="mb-6 text-center">
        <h2 class="text-3xl font-bold text-amber-400 mb-4">RIDDLE ME THIS</h2>
        <p class="text-gray-300 mb-2">
          My uncle has a sister who is not my aunt, who is she?
        </p>
      </header>
      <main>
        <% if (mode === "success") { %>
        <div class="bg-green-800 text-green-200 font-semibold text-center px-4 py-3 rounded-lg mb-4" role="alert">
          <%= message %>
        </div>
        <a
          href="/passcode"
          class="block text-center bg-amber-500 text-gray-900 px-4 py-3 rounded-lg font-bold hover:bg-amber-400 transition-colors"
        >
          Go Back to Enter Passcode
        </a>
        <% } else { %> <% if (message) { %>
        <div class="bg-red-800 text-red-200 font-semibold text-center px-4 py-3 rounded-lg mb-4" role="alert">
          <%= message %>
        </div>
        <% } %>
        <form
          action="/passcode/riddle"
          method="POST"
          class="w-full space-y-6"
        >
          <div>
            <input
              type="text"
              name="riddle"
              placeholder="Your Answer"
              class="w-full border border-gray-600 focus:ring-2 focus:ring-amber-500 focus:border-amber-500 rounded-lg px-4 py-3 text-gray-200 placeholder-gray-400 focus:outline-none bg-gray-700"
            />
          </div>
          <button
            type="submit"
            class="w-full bg-amber-500 text-gray-900 px-6 py-3 rounded-lg font-bold shadow-md hover:bg-amber-400 transition-colors"
          >
            Answer
          </button>
        </form>
        <% } %>
      </main>
    </section>
  </body>
</html>
